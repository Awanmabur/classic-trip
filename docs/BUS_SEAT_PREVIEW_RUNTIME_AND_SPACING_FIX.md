# Bus Seat Preview Runtime and Spacing Fix

Fixed the public listing preview crash caused by an undefined `returnSeatIndex` variable. Public and dashboard bus seat previews now use clean numeric `Seat No. N` labels, wider seats, and improved spacing while preserving the requested colors: booked red, held green, available blue, selected yellow, blocked/maintenance orange.
