# Bus Seat Map Page Surrounding Redesign Pass

This pass keeps the good parts of the Seat Maps page: the visual bus seat grid and the scoped list table below it.

Changes made:
- Redesigned the surrounding Seat Maps page layout only.
- Added a cleaner hero/header for Bus Seat Operations.
- Moved primary actions into a tidy action cluster.
- Added compact summary cards above the selector.
- Reworked the route/schedule selector into a clean command bar.
- Kept the selected route/schedule behavior.
- Kept the visual seat preview grid and table below it.
- Moved vehicle seat-template tools into a collapsible section so they do not clutter the main seat-map workflow.
- Improved responsive behavior for the selector, stats, and action buttons.

Checks run:
- npm run check
- npm run check:dashboards
